Page index 
{
	Link toPageOne to pageOne
	Graph testGraph a
}

Page pageOne
{
	Link toHomePage to index
	Graph testGraph a
	Graph testGraph2 a
	Graph testGraph3 c
	Graph testGraph4 c
	
}

Datasource a {
	Dimensions:
		Formula x(x, n) = x using lol[x] as x and using lol[k] as n,
		Formula y(x, y) = y using lol[y] as x and using lol[x] as y
}

Datasource c {
	Dimensions:
		Formula g(z) = z using a[y] as z
}


Schema schemaA {
	SchemaType = JSON
	Selector as x {
		"0" -> "Readings" 
	}
}

GetPoint lol {
	url "http://10.123.3.12:8079/api/data/uuid/bec6ad6b-753a-548d-bb8c-4e03f4d67b2e?endtime=1463909610000&limit=100"
	Headers {
		"type" : "Best"
	}
	use Schema schemaA
}